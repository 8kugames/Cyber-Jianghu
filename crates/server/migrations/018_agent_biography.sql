-- ============================================================================
-- 018_agent_biography.sql
-- ============================================================================
-- agents 表新增 biography 字段，存储角色纪传体传记
-- 由 agent 端在角色死亡/归隐时生成并回传
-- ============================================================================

ALTER TABLE agents ADD COLUMN IF NOT EXISTS biography TEXT;

COMMENT ON COLUMN agents.biography IS '纪传体角色传记（死亡/归隐时由 agent LLM 生成，10-2000字）';
