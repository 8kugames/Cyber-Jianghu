-- ============================================================================
-- 021_agents_model_id.sql
-- ============================================================================
-- agents.model_id -- 角色注册时使用的 LLM 模型 ID（用于角色面板 / 经历日志展示）
-- 可空，兼容存量数据。
-- ============================================================================

ALTER TABLE agents ADD COLUMN IF NOT EXISTS model_id VARCHAR(100);

COMMENT ON COLUMN agents.model_id IS '角色注册时上报的 LLM 模型 ID（如 glm-4、gpt-4o）';
