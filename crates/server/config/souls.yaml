version: "0.0.1"

data:
  souls:
    fuxi:
      display_name: "伏羲"
      governance_role: "evolution"
      review_policy:
        hard_approve_if: []
        hard_reject_if: []
        soft_concern_if: []
      source_bindings:
        capability_manifest: true
      system_prompt_template: "fuxi_review_prompt"

  # 神农、轩辕 — Phase 2 实施，当前仅挂起
  # shennong:
  #   display_name: "神农"
  #   governance_role: "survival"
  #   review_policy:
  #     hard_approve_if:
  #       - metric: "agent_survival_rate_24h"
  #         operator: ">"
  #         threshold: 0.7
  #         reason: "Agent 存活率健康"
  #     hard_reject_if:
  #       - metric: "agent_survival_rate_24h"
  #         operator: "<"
  #         threshold: 0.3
  #         reason: "生存压力已饱和"
  #     soft_concern_if:
  #       - metric: "resource_yield_rate"
  #         operator: "<"
  #         threshold: 0.5
  #         reason: "资源产出低迷，谨慎评估新能力的资源消耗"
  #   source_bindings:
  #     resource_indicators: true
  #     survival_indicators: true
  #   system_prompt_template: "shennong_review_prompt"
  #
  # xuanyuan:
  #   display_name: "轩辕"
  #   governance_role: "order"
  #   review_policy:
  #     hard_approve_if: []
  #     hard_reject_if:
  #       - effect_ref_matches: ["instant_kill", "force_trade"]
  #         reason: "秩序红线"
  #     soft_concern_if:
  #       - effect_ref_matches: ["combat.*"]
  #         reason: "战斗能力影响秩序"
  #         requires: []
  #   source_bindings:
  #     order_indicators: true
  #   system_prompt_template: "xuanyuan_review_prompt"

  topic_to_soul:
    evolution: "fuxi"
    # resource: "shennong"   # Phase 2
    # order: "xuanyuan"      # Phase 2

  topic_priority:
    evolution: 0
    # resource: 1            # Phase 2
    # order: 2               # Phase 2

  classifier:
    confidence_threshold: 0.6
    default_fallback_topic: "evolution"

  review:
    timeout_secs: 1800
    dissent_log_threshold: 3
    approve_threshold: 2
    reject_threshold: 2
    poll_interval_secs: 60
