# ============================================================================
# Cyber-Jianghu Server Docker Compose (Development)
# 服务端开发环境配置
# ============================================================================
#
# 使用方式：
# 1. 复制 .env.example 为 .env 并修改配置
# 2. 启动服务：docker compose up -d
# 3. 查看日志：docker compose logs -f server
# 4. 停止服务：docker compose down
# ============================================================================

services:
  # ============================================================================
  # PostgreSQL 数据库
  # ============================================================================
  postgres:
    image: postgres:15-alpine
    container_name: cyber-jianghu-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: cyber_jianghu
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${DB_PASSWORD:-changeme}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      # 挂载迁移文件（按文件名排序执行）
      - ./migrations/001_initial_schema.sql:/docker-entrypoint-initdb.d/001_initial_schema.sql:ro
      - ./migrations/002_jsonb_attributes.sql:/docker-entrypoint-initdb.d/002_jsonb_attributes.sql:ro
      - ./migrations/003_ground_items.sql:/docker-entrypoint-initdb.d/003_ground_items.sql:ro
      - ./migrations/004_drop_legacy_columns.sql:/docker-entrypoint-initdb.d/004_drop_legacy_columns.sql:ro
      - ./migrations/005_devices.sql:/docker-entrypoint-initdb.d/005_devices.sql:ro
      - ./migrations/006_agents_device_link.sql:/docker-entrypoint-initdb.d/006_agents_device_link.sql:ro
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - cyber-jianghu-network

  # ============================================================================
  # Rust 服务端
  # ============================================================================
  server:
    build:
      context: ../..
      dockerfile: ./crates/server/Dockerfile
    container_name: cyber-jianghu-server
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      # 服务端配置
      SERVER_HOST: 0.0.0.0
      SERVER_PORT: 23333

      # Tick 引擎配置
      TICK_DURATION_SECS: ${TICK_DURATION_SECS:-60}

      # 管理后台访问凭证
      ADMIN_READ_TOKEN: ${ADMIN_READ_TOKEN:-}
      ADMIN_WRITE_TOKEN: ${ADMIN_WRITE_TOKEN:-}

      # 数据库配置
      DATABASE_URL: postgres://postgres:${DB_PASSWORD:-changeme}@postgres:5432/cyber_jianghu

      # 日志级别
      RUST_LOG: ${RUST_LOG:-info}

      # 环境标识
      ENVIRONMENT: ${ENVIRONMENT:-development}
    ports:
      - "${SERVER_PORT:-23333}:23333"
    volumes:
      # 配置文件挂载（数据驱动架构 - 支持热插拔）
      - ./config:/app/config
      # 日志持久化
      - ./logs:/app/logs
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:23333/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    networks:
      - cyber-jianghu-network

# ============================================================================
# 网络配置
# ============================================================================
networks:
  cyber-jianghu-network:
    driver: bridge
    name: cyber-jianghu-network

# ============================================================================
# 数据卷配置
# ============================================================================
volumes:
  postgres_data:
    name: cyber-jianghu-postgres-data
