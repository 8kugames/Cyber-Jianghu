# 动作系统

## 动作类型

> **注意**：动作类型不是硬编码枚举，而是通过 `config/actions.yaml` 数据驱动定义。以下类别仅为常见动作的逻辑分组。

| 类型 | 说明 | 示例 |
|------|------|------|
| basic | 基础动作 | idle, move, look |
| social | 社交动作 | talk, emote |
| combat | 战斗动作 | attack, defend |
| item | 物品动作 | equip, use, drop |
| crafting | 制作动作 | craft, cook |

实际支持的完整动作列表参见 `config/actions.yaml`。

## 动作执行器

### 基础动作执行器

```rust
async fn execute_basic(action: &Action, state: &mut WorldState) -> Result<Vec<Event>> {
    match action.type {
        "idle" => execute_idle(state),
        "move" => execute_move(state, &action.params),
        _ => Err(ActionError::UnknownAction),
    }
}
```

### 战斗动作执行器

```rust
async fn execute_combat(action: &Action, state: &mut WorldState) -> Result<Vec<Event>> {
    // 伤害计算
    // 防御处理
    // 死亡检测
}
```

### 交互动作执行器

```rust
async fn execute_interaction(action: &Action, state: &mut WorldState) -> Result<Vec<Event>> {
    // NPC 对话
    // 物体交互
    // 环境触发
}
```
