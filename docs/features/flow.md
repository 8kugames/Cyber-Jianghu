# Cyber-Jianghu 架构图谱与数据流

**日期**: 2026-04-09
**版本**: v0.3

---

## 一、项目概览

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           Cyber-Jianghu MMO-MAS                              │
│                                                                              │
│   ┌─────────────────┐         ┌─────────────────┐         ┌─────────────────┐ │
│   │   OpenClaw      │         │   Server        │         │   Agent         │ │
│   │   (外部调度器)   │◄──────►│   (天道/服务端)   │◄──────►│   (众生/客户端)  │ │
│   │                 │  WS/HTTP│                 │  WS/HTTP│                 │ │
│   │                 │         │  Tick Engine     │         │  CognitiveSoul  │ │
│   │                 │         │  WebSocket       │         │  ReflectorSoul  │ │
│   │                 │         │  HTTP API        │         │  Memory System  │ │
│   │                 │         │  PostgreSQL      │         │  LLM Client     │ │
│   └─────────────────┘         └─────────────────┘         └─────────────────┘ │
│           │                           │                           │          │
│           └───────────────────────────┴───────────────────────────┘          │
│                              Protocol Layer                                    │
│                        (cyber_jianghu_protocol)                                │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 二、宏观架构

### 2.1 系统分层

```
┌────────────────────────────────────────────────────────────────────────────┐
│                              表现层 (Presentation)                           │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐          │
│  │   Admin Web UI   │  │   Agent SDK      │  │   OpenClaw       │          │
│  │   /admin/*      │  │   HTTP API       │  │   External       │          │
│  │   (Axum Static) │  │   /api/v1/*     │  │   Scheduler      │          │
│  └──────────────────┘  └──────────────────┘  └──────────────────┘          │
└────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌────────────────────────────────────────────────────────────────────────────┐
│                             协议层 (Protocol)                                │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │                     cyber_jianghu_protocol                             │  │
│  │  ServerMessage ◄───────────────────────────────────► ClientMessage   │  │
│  │       │                                        │                      │  │
│  │       │  WorldState, Intent, DialogueMessage    │                      │  │
│  │       ▼                                        ▼                      │  │
│  │  [Registered, WorldState, ActionUpdate,        [Intent, Dialogue]      │  │
│  │   Dialogue, Error, DeathNotification,           [Pong]                 │  │
│  │   ActionResult, PrivateDialogueRecord,         ]                      │  │
│  │   ImmediateEvent, AgentDied]                    ]                      │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌────────────────────────────────────────────────────────────────────────────┐
│                             服务端 (Server)                                  │
│                                                                              │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │                          Tick Engine (天道)                            │  │
│  │                                                                      │  │
│  │   ┌─────────────┐    ┌─────────────┐    ┌─────────────┐            │  │
│  │   │ Broadcaster │───►│   Sleep     │───►│  Settlement │            │  │
│  │   │ (广播开单)   │    │ (收集窗口)   │    │  (结算关单)  │            │  │
│  │   └─────────────┘    └─────────────┘    └─────────────┘            │  │
│  │          │                                      │                    │  │
│  │          │                                      ▼                    │  │
│  │          │              ┌─────────────────────────────────┐          │  │
│  │          │              │        StateProcessor           │          │  │
│  │          │              │  ┌─────────┐ ┌─────────┐       │          │  │
│  │          │              │  │Resolver │ │Executor │       │          │  │
│  │          │              │  │(验证)   │ │(执行)   │       │          │  │
│  │          │              │  └────┬────┘ └────┬────┘       │          │  │
│  │          │              │       │           │             │          │  │
│  │          │              │       ▼           ▼             │          │  │
│  │          │              │  ┌─────────────────────┐       │          │  │
│  │          │              │  │    Mutators         │       │          │  │
│  │          │              │  │ Attribute/Inventory │       │          │  │
│  │          │              │  │ /Location          │       │          │  │
│  │          │              │  └─────────────────────┘       │          │  │
│  │          │              └─────────────────────────────────┘          │  │
│  │          │                      │                                  │  │
│  │          │                      ▼                                  │  │
│  │          │              ┌─────────────────────────────────┐          │  │
│  │          │              │      Decay & Death             │          │  │
│  │          │              │  - apply_decay()                │          │  │
│  │          │              │  - 环境伤害                      │          │  │
│  │          │              │  - 立即推送 DeathNotification   │          │  │
│  │          │              └─────────────────────────────────┘          │  │
│  │          │                      │                                  │  │
│  │          │                      ▼                                  │  │
│  │          │              ┌─────────────────────────────────┐          │  │
│  │          │              │      Persistence               │          │  │
│  │          │              │  - save_tick_log               │          │  │
│  │          │              │  - persist_states              │          │  │
│  │          │              │  - batch_insert_action_logs   │          │  │
│  │          │              └─────────────────────────────────┘          │  │
│  └──────────┼───────────────────────────────────────────────────────┘  │
│             │                                                               │
│             ▼                                                               │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │                        WebSocket Handler                               │  │
│  │   ┌──────────────┐  ┌──────────────┐  ┌──────────────┐             │  │
│  │   │ handle_ws()  │  │ handle_intent │  │   Heartbeat  │             │  │
│  │   │ (连接管理)    │  │ (意图接收)    │  │   (Ping/Pong)│            │  │
│  │   └──────────────┘  └──────────────┘  └──────────────┘             │  │
│  │                                                                       │  │
│  │   ┌──────────────────────────────────────────────────────────────┐  │  │
│  │   │            Immediate Actions (即时动作)                       │  │  │
│  │   │   speak → 立即广播给同场景 Agent                              │  │  │
│  │   │   whisper → 立即创建 Dialogue Session                         │  │  │
│  │   └──────────────────────────────────────────────────────────────┘  │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │                        HTTP API Handlers                              │  │
│  │   /api/v1/agent/*  /api/dashboard/*  /api/config/*                  │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │                         Data Layer                                    │  │
│  │   ┌──────────────┐  ┌──────────────┐  ┌──────────────┐             │  │
│  │   │  PostgreSQL  │  │   GameData   │  │   ItemCache  │             │  │
│  │   │  (持久化)     │  │   (配置)     │  │   (物品缓存)  │             │  │
│  │   └──────────────┘  └──────────────┘  └──────────────┘             │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌────────────────────────────────────────────────────────────────────────────┐
│                              客户端 (Agent)                                  │
│                                                                              │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │                        Runtime Modes                                   │  │
│  │   ┌─────────────────────┐      ┌─────────────────────┐             │  │
│  │   │   Cognitive Mode     │      │    Claw Mode        │             │  │
│  │   │   (内置LLM决策)       │      │    (外部调度器)      │             │  │
│  │   └─────────────────────┘      └─────────────────────┘             │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │                    Three-Soul Architecture (三魂)                      │  │
│  │                                                                      │  │
│  │   ┌───────────────────────────┐  ┌─────────────────┐               │  │
│  │   │   ActorSoul               │  │  ReflectorSoul   │               │  │
│  │   │   (人魂/行动之魂)          │  │  (天魂/守护之魂)  │               │  │
│  │   │                           │  │                 │               │  │
│  │   │  ┌───────────┐            │  │  ┌───────────┐ │               │  │
│  │   │  │Cognitive  │            │  │  │RuleEngine │ │               │  │
│  │   │  │Engine     │            │  │  │Validator  │ │               │  │
│  │   │  └─────┬─────┘            │  │  └─────┬─────┘ │               │  │
│  │   │        ▼                  │  │        ▼       │               │  │
│  │   │  ┌───────────────┐        │  │  ┌─────────┐   │               │  │
│  │   │  │action_type+ID │────────│─│─►│action_  │   │               │  │
│  │   │  │(直连WorldState)│        │  │  │type val │   │               │  │
│  │   │  └───────┬───────┘        │  │  └─────────┘   │               │  │
│  │   │          │                 │  │                │               │  │
│  │   │          │                 │  │ validate_with_ │               │  │
│  │   │   结构化 Intent            │  │  reflector     │               │  │
│  │   └──────────┼────────────────┘  └───────┬────────┘               │  │
│  │              │                             │                        │  │
│  │              └─────────────────────────────┘                        │  │
│  │                             ▼                                     │  │
│  │   ┌───────────────────────────────────────────────┐              │  │
│  │   │              Intent Submission                 │              │  │
│  │   │         (WebSocket → Server)                  │              │  │
│  │   └───────────────────────────────────────────────┘              │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │                        Memory System (三级记忆)                        │  │
│  │   ┌───────────┐    ┌───────────┐    ┌───────────┐                   │  │
│  │   │  Working  │───►│ Episodic  │───►│ Semantic  │                   │  │
│  │   │  Memory   │    │  Memory   │    │  Memory   │                   │  │
│  │   │ (短期上下文) │    │ (事件序列) │    │ (向量检索) │                   │  │
│  │   └───────────┘    └───────────┘    └───────────┘                   │  │
│  │          │               │               │                           │  │
│  │          └───────────────┴───────────────┘                           │  │
│  │                          │                                           │  │
│  │                          ▼                                           │  │
│  │                 ┌─────────────────┐                                 │  │
│  │                 │  Forgetting     │                                 │  │
│  │                 │  Scheduler      │                                 │  │
│  │                 │  (艾宾浩斯遗忘) │                                 │  │
│  │                 └─────────────────┘                                 │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │                      Components                                       │  │
│  │   ┌─────────────┐  ┌─────────────┐  ┌─────────────┐                │  │
│  │   │   Persona   │  │  Relationship│  │     LLM     │                │  │
│  │   │  (人设+寿命) │  │   (关系存储)  │  │  (模型抽象)  │                │  │
│  │   └─────────────┘  └─────────────┘  └─────────────┘                │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │                      Infrastructure                                   │  │
│  │   ┌─────────────────┐            ┌─────────────────┐                 │  │
│  │   │  WebSocket      │            │    HTTP API     │                 │  │
│  │   │  Client        │            │    Server       │                 │  │
│  │   │  (连接服务端)   │            │    (Web Panel)  │                 │  │
│  │   └─────────────────┘            └─────────────────┘                 │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────────────────────────┘
```

---

## 三、模块依赖关系

### 3.1 Server Crate 依赖图

```
┌────────────────────────────────────────────────────────────────────────────┐
│                           crates/server/src                                │
└────────────────────────────────────────────────────────────────────────────┘
                                     │
          ┌──────────────────────────┼──────────────────────────┐
          │                          │                          │
          ▼                          ▼                          ▼
┌─────────────────┐      ┌─────────────────┐      ┌─────────────────┐
│     main.rs     │      │     lib.rs      │      │   config.rs     │
│   (入口+状态)    │      │   (模块导出)     │      │   (配置加载)    │
└────────┬────────┘      └────────┬────────┘      └─────────────────┘
         │                        │
         │           ┌─────────────┼─────────────┐
         │           │             │             │
         ▼           ▼             ▼             ▼
┌─────────────────┐ ┌─────────────────────────────────────────────────────┐
│     state.rs    │ │                    handlers/                        │
│  (AppState)     │ │  ┌───────┐ ┌───────┐ ┌───────┐ ┌───────┐        │
└────────┬────────┘ │  │ agent │ │ auth  │ │dashboard│ │system │        │
         │          │  └───────┘ └───────┘ └───────┘ └───────┘        │
         │          │  ┌───────┐ ┌───────┐                               │
         │          │  │context│ │validation│                              │
         │          │  └───────┘ └───────┘                               │
         │          └─────────────────────────────────────────────────────┘
         │
         ▼
┌────────────────────────────────────────────────────────────────────────────┐
│                              核心模块                                         │
│                                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │    tick/     │  │   websocket/ │  │    db/       │  │  game_data/  │  │
│  │              │  │              │  │              │  │              │  │
│  │  scheduler   │  │  handler     │  │  agent_ops   │  │  loader      │  │
│  │  broadcaster │  │  connection  │  │  state_ops   │  │  registry    │  │
│  │  processor   │  │  broadcast   │  │  item_ops    │  │  types       │  │
│  │  decay       │  │              │  │  ground_item │  │  formula_eng │  │
│  │  persistence │  │              │  │              │  │              │  │
│  │  event_mgr   │  │              │  │              │  │              │  │
│  │  intent_coll │  │              │  │              │  │              │  │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘  │
│         │                 │                 │                 │          │
└─────────┼─────────────────┼─────────────────┼─────────────────┼──────────┘
          │                 │                 │                 │
          ▼                 ▼                 ▼                 ▼
┌────────────────────────────────────────────────────────────────────────────┐
│                              支撑模块                                         │
│                                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │   actions/   │  │  dialogue/   │  │   models/   │  │   items/    │  │
│  │              │  │              │  │              │  │              │  │
│  │  executor    │  │  session_mgr │  │  agent.rs   │  │  registry   │  │
│  │  validator   │  │  session.rs  │  │  state_*    │  │  system.rs  │  │
│  │  types       │  │  types       │  │  tick.rs    │  │  types      │  │
│  │              │  │              │  │  actions.rs │  │              │  │
│  └──────────────┘  └──────────────┘  │  items.rs   │  └──────────────┘  │
│                                       │  validation │                     │
│  ┌──────────────┐                     └──────────────┘                     │
│  │ inventory/  │                                                        │
│  │              │                                                        │
│  │  manager     │                                                        │
│  │  types       │                                                        │
│  │  error       │                                                        │
│  └──────────────┘                                                        │
└────────────────────────────────────────────────────────────────────────────┘
```

### 3.2 Agent Crate 依赖图

```
┌────────────────────────────────────────────────────────────────────────────┐
│                            crates/agent/src                                │
└────────────────────────────────────────────────────────────────────────────┘
                                     │
          ┌──────────────────────────┼──────────────────────────┐
          │                          │                          │
          ▼                          ▼                          ▼
┌─────────────────┐      ┌─────────────────┐      ┌─────────────────┐
│   lib.rs        │      │     models.rs   │      │    config.rs    │
│   (模块导出)     │      │   (数据类型)    │      │   (配置结构)    │
└────────┬────────┘      └─────────────────┘      └─────────────────┘
         │
         ├────────────────────────────────────────────────────────────────┐
         │                                                                │
         ▼                                                                ▼
┌────────────────────────────────────────────────────────────────────────────┐
│                              核心模块                                         │
│                                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │    core/    │  │     soul/    │  │  component/  │  │   runtime/   │  │
│  │              │  │              │  │              │  │              │  │
│  │  agent.rs   │  │   actor/     │  │  memory/    │  │   cognitive  │  │
│  │  builder    │  │    engine    │  │  persona/   │  │   claw       │  │
│  │  lifecycle  │  │    chain     │  │  social/    │  │   decision   │  │
│  │  tools      │  │    stages    │  │    llm/     │  │              │  │
│  │  utils      │  │  narrative   │  │              │  │              │  │
│  │              │  │              │  │              │  │              │  │
│  │              │  │  reflector/  │  │              │  │              │  │
│  │              │  │  validator   │  │              │  │              │  │
│  │              │  │  rule_engine │  │              │  │              │  │
│  │              │  │  store       │  │              │  │              │  │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘  │
│         │                 │                 │                 │          │
└─────────┼─────────────────┼─────────────────┼─────────────────┼──────────┘
          │                 │                 │                 │
          ▼                 ▼                 ▼                 ▼
┌────────────────────────────────────────────────────────────────────────────┐
│                            基础设施层                                        │
│                                                                              │
│  ┌──────────────────────────────────────┐  ┌──────────────────────────┐   │
│  │             infra/                    │  │         bin/            │   │
│  │                                      │  │                          │   │
│  │  ┌─────────────┐  ┌─────────────┐   │  │  cyber-jianghu-agent.rs  │   │
│  │  │    api/     │  │  transport/ │   │  │     (CLI 入口)           │   │
│  │  │             │  │             │   │  │                          │   │
│  │  │  handlers   │  │  websocket  │   │  └──────────────────────────┘   │
│  │  │  services   │  │  client     │   │                                  │
│  │  │  dto        │  │             │   │                                  │
│  │  │  context    │  │             │   │                                  │
│  │  └─────────────┘  └─────────────┘   │                                  │
│  └──────────────────────────────────────┘                                  │
└────────────────────────────────────────────────────────────────────────────┘
```

---

## 四、完整数据流图

### 4.1 Tick 周期完整流程

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              Tick N 完整生命周期                               │
└─────────────────────────────────────────────────────────────────────────────┘

    ┌─────────────────────────────────────────────────────────────────────┐
    │ 阶段 1: 开单广播 (Broadcast Open)                                     │
    │ Tick Engine                                                          │
    │                                                                       │
    │  scheduler.run()                                                     │
    │       │                                                              │
    │       ├──► check_and_reload_actions()  [热重载检查]                    │
    │       │                                                              │
    │       ├──► interval.tick()  [等待周期时间]                            │
    │       │                                                              │
    │       ├──► calculate_tick_id_from_time()  [Unix时间戳 - 游戏纪元]     │
    │       │                                                              │
    │       ├──► atomic_store(accepting_tick_id, N)  ◄── 开单              │
    │       │                                                              │
    │       ├──► calculate_deadline_abs_ms()  [绝对时间戳]                   │
    │       │                                                              │
    │       ├──► persistence::load_agent_states()                           │
    │       │         │                                                    │
    │       │         ▼                                                    │
    │       │    ┌─────────────────────────────────────────┐                 │
    │       │    │           PostgreSQL                   │                 │
    │       │    │  SELECT * FROM agent_states            │                 │
    │       │    │  WHERE agent_id IN (                  │                 │
    │       │    │    SELECT DISTINCT ON(agent_id)       │                 │
    │       │    │    agent_id FROM agent_states         │                 │
    │       │    │    ORDER BY agent_id, tick_id DESC    │                 │
    │       │    │  ) AND is_alive = true               │                 │
    │       │    └─────────────────────────────────────────┘                 │
    │       │         │                                                    │
    │       │         ▼                                                    │
    │       │    ┌─────────────┐                                           │
    │       │    │  Vec<Agent> │                                           │
    │       │    │  _states    │                                           │
    │       │    └──────┬──────┘                                           │
    │       │           │                                                  │
    │       ├──► broadcaster.broadcast_states()                              │
    │       │         │                                                    │
    │       │         ▼                                                    │
    │       │    ┌─────────────────────────────────────────┐                 │
    │       │    │      for each agent in agent_states    │                 │
    │       │    │           │                            │                 │
    │       │    │           │  [预加载: batch 查询]      │                 │
    │       │    │           │    InventoryManager::get_all_items_batch()  │
    │       │    │           │    db::get_ground_items_by_nodes()          │
    │       │    │           │         │                               │
    │       │    │           │         ▼                               │
    │       │    │           │    PostgreSQL (batch SELECT)             │
    │       │    │           │         │                               │
    │       │    │           ├──► build_world_state()                    │
    │       │    │           │         │                               │
    │       │    │           │         ├──► compute_game_time()          │
    │       │    │           │         ├──► LocationRegistry::get_node()  │
    │       │    │           │         └──► get_attributes_for_protocol() │
    │       │    │           │                                           │
    │       │    │           └──► send_world_state()                      │
    │       │    │                     │                                  │
    │       │    │                     ▼                                  │
    │       │    │              ┌──────────────┐                        │
    │       │    │              │ WebSocket    │                        │
    │       │    │              │ Connection   │                        │
    │       │    │              └──────────────┘                        │
    │       │    └─────────────────────────────────────────┘              │
    │       │                                                              │
    └───────┼──────────────────────────────────────────────────────────────┘
            │
            ▼
    ┌─────────────────────────────────────────────────────────────────────┐
    │ 阶段 2: 收集窗口 (Collection Window)                                 │
    │                                                                       │
    │  tokio::time::sleep(collection_window_secs)                           │
    │       │                                                              │
    │       │  [在此期间，Agent 可通过 WebSocket 提交 Intent]                │
    │       │                                                              │
    │       │  Agent Side:                                                  │
    │       │    ┌─────────────────────────────────────────────────────┐   │
    │       │    │ AgentClient                                          │   │
    │       │    │   │                                                  │   │
    │       │    │   ├──► 接收 WorldState (ServerMessage::WorldState)    │   │
    │       │    │   │                                                  │   │
    │       │    │   ├──► ActorSoul::think(tick_id, agent_id, memory_context)   │   │
    │       │    │   │         │                                        │   │
    │       │    │   │         ├──► NarrativeGenerator::generate() (地魂叙事注入) │   │
    │       │    │   │         ├──► CognitiveChain::run()               │   │
    │       │    │   │         │     ├──► Stage 1: 感知-动机             │   │
    │       │    │   │         │     ├──► Stage 2: 计划-决策             │   │
    │       │    │   │         │     └──► 合并 LLM 调用                   │   │
    │       │    │   │         └──► Intent 生成                           │   │
    │       │    │   │                                                  │   │
    │       │    │   ├──► ReflectorSoul::validate()                      │   │
    │       │    │   │         │                                        │   │
    │       │    │   │         ├──► RuleEngine::validate()              │   │
    │       │    │   │         └──► 审查结果 (approved/rejected)           │   │
    │       │    │   │                                                  │   │
    │       │    │   └──► send_intent()                                 │   │
    │       │    │             │                                         │   │
    │       │    │             ▼                                         │   │
    │       │    │     ClientMessage::Intent { tick_id: N, ... }         │   │
    │       │    └─────────────────────────────────────────────────────┘   │
    │       │                                                              │
    └───────┼──────────────────────────────────────────────────────────────┘
            │
            ▼
    ┌─────────────────────────────────────────────────────────────────────┐
    │ 阶段 3: 关单 (Collection Closed)                                    │
    │                                                                       │
    │  atomic_store(accepting_tick_id, 0)  ◄── 关单                       │
    │       │                                                              │
    └───────┼──────────────────────────────────────────────────────────────┘
            │
            ▼
    ┌─────────────────────────────────────────────────────────────────────┐
    │ 阶段 4: 结算 (Settlement)                                          │
    │                                                                       │
    │  execute_tick_settlement(N)                                          │
    │       │                                                              │
    │       ├──► db::create_tick_log()                                    │
    │       │                                                              │
    │       ├──► execute_tick_inner(N)                                     │
    │       │         │                                                    │
    │       │         ▼                                                    │
    │       │    ┌─────────────────────────────────────────┐               │
    │       │    │ 4.1 加载状态 (Phase 1)                   │               │
    │       │    │     persistence::load_agent_states()      │               │
    │       │    └─────────────────────────────────────────┘               │
    │       │         │                                                    │
    │       │         ▼                                                    │
    │       │    ┌─────────────────────────────────────────┐               │
    │       │    │ 4.2.1 意图结算 (Phase 2.1)              │               │
    │       │    │                                         │               │
    │       │    │  intent_collector.collect_intents()     │               │
    │       │    │         │                               │               │
    │       │    │         ├──► take_intents_for_tick(N)   │               │
    │       │    │         │    (从 IntentManager 取出)     │               │
    │       │    │         │                               │               │
    │       │    │         ▼                               │               │
    │       │    │  state_processor.process_intents()       │               │
    │       │    │         │                               │               │
    │       │    │         ├──► IntentResolver::validate()  │               │
    │       │    │         │    (actions::validator)        │               │
    │       │    │         │         │                      │               │
    │       │    │         │         ├──► 动作存在性检查     │               │
    │       │    │         │         ├──► 属性消耗检查      │               │
    │       │    │         │         ├──► 物品需求检查      │               │
    │       │    │         │         └──► 位置合法性检查    │               │
    │       │    │         │                               │               │
    │       │    │         ├──► ActionExecutor::execute()  │               │
    │       │    │         │    (executor/basic/combat)    │               │
    │       │    │         │         │                      │               │
    │       │    │         │         ├──► consume_requirements()            │
    │       │    │         │         ├──► match action_type:  │               │
    │       │    │         │         │    ├──► idle        │               │
    │       │    │         │         │    ├──► speak      │               │
    │       │    │         │         │    ├──► move       │               │
    │       │    │         │         │    ├──► attack     │               │
    │       │    │         │         │    ├──► use/eat/drink              │
    │       │    │         │         │    └──► ...       │               │
    │       │    │         │         │                               │               │
    │       │    │         │         └──► apply_generic_effects()           │
    │       │    │         │                               │               │
    │       │    │         └──► apply_state_change()        │               │
    │       │    │              (processor/executor.rs)     │               │
    │       │    │                    │                      │               │
    │       │    │                    ├──► AttributeMutator              │
    │       │    │                    │    (HP/体力/饥饿)      │               │
    │       │    │                    ├──► InventoryMutator                │
    │       │    │                    │    (物品转移)            │               │
    │       │    │                    └──► LocationMutator                 │
    │       │    │                         (位置移动)            │               │
    │       │    │                                                       │               │
    │       │    │  验证失败 → validation_errors 通知 ServerMessage::Error │
    │       │    └─────────────────────────────────────────┘               │
    │       │         │                                                    │
    │       │         ▼                                                    │
    │       │    ┌─────────────────────────────────────────┐               │
    │       │    │ 4.2.2 衰减与环境伤害 (Phase 2.2)          │               │
    │       │    │                                         │               │
    │       │    │  decay::apply_decay_and_env_damage()    │               │
    │       │    │         │                               │               │
    │       │    │         ├──► for each agent:            │               │
    │       │    │         │    agent_state.apply_decay()   │               │
    │       │    │         │         │                      │               │
    │       │    │         │         ├──► get_decaying_attributes()        │
    │       │    │         │         │    (hunger, thirst)  │               │
    │       │    │         │         ├──► get_recovering_attributes()       │
    │       │    │         │         │    (stamina)         │               │
    │       │    │         │         ├──► apply_change()    │               │
    │       │    │         │         └──► check_death_condition()          │
    │       │    │         │                               │               │
    │       │    │         ├──► 环境伤害                    │               │
    │       │    │         │    LocationRegistry::get_node()                │
    │       │    │         │    .environmental_damage > 0?                  │
    │       │    │         │                               │               │
    │       │    │         └──► 死亡处理                   │               │
    │       │    │              │                               │               │
    │       │    │              ├──► 立即推送 DeathNotification              │
    │       │    │              │    send_agent_died_notification()         │
    │       │    │              │                               │               │
    │       │    │              ├──► 断开 WebSocket 连接                   │
    │       │    │              │    connection_manager.remove()            │
    │       │    │              │                               │               │
    │       │    │              ├──► 清空背包 (InventoryManager)            │
    │       │    │              │    + 死亡掉落物品到地面                   │
    │       │    │              │                               │               │
    │       │    │              └──► UPDATE agents SET status='dead'        │
    │       │    └─────────────────────────────────────────┘               │
    │       │         │                                                    │
    │       │         ▼                                                    │
    │       │    ┌─────────────────────────────────────────┐               │
    │       │    │ 4.3 统计与记录 (Phase 3)                 │               │
    │       │    │                                         │               │
    │       │    │  db::get_intent_timeout_stats()          │               │
    │       │    │  (每10 tick记录一次)                      │               │
    │       │    └─────────────────────────────────────────┘               │
    │       │         │                                                    │
    │       │         ▼                                                    │
    │       │    ┌─────────────────────────────────────────┐               │
    │       │    │ 4.4 持久化 (Phase 4)                   │               │
    │       │    │                                         │               │
    │       │    │  persistence::persist_states()           │               │
    │       │    │         │                               │               │
    │       │    │         ├──► INSERT INTO agent_states   │               │
    │       │    │         │                               │               │
    │       │    │         ├──► db::batch_insert_action_logs()             │
    │       │    │         │                               │               │
    │       │    │         └──► persistence::save_tick_log()                │
    │       │    │              (文件)                       │               │
    │       │    └─────────────────────────────────────────┘               │
    │       │                                                              │
    │       ├──► tick_log.complete()                                      │
    │       │                                                              │
    │       └──► dialogue_manager.close_all_sessions()                     │
    │                                                                     │
    └─────────────────────────────────────────────────────────────────────┘
            │
            ▼
    ┌─────────────────────────────────────────────────────────────────────┐
    │ Tick N 完成 → 进入 Tick N+1 循环                                      │
    └─────────────────────────────────────────────────────────────────────┘
```

### 4.2 WebSocket 连接与消息处理流程

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                       WebSocket 连接与消息处理                                │
└─────────────────────────────────────────────────────────────────────────────┘

    Agent                                      Server
      │                                           │
      │  1. WebSocket 升级请求                      │
      │  GET /ws?device_id=xxx&token=yyy          │
      │ ─────────────────────────────────────────►
      │                                           │
      │                                           ├──► verify_device_token()
      │                                           │         │
      │                                           │         ▼
      │                                           │    PostgreSQL: devices 表
      │                                           │
      │                                           ├──► get_agent_by_device_id()
      │                                           │         │
      │                                           │         ▼
      │                                           │    PostgreSQL: agents 表
      │                                           │
      │                                           ├──► 添加到 ConnectionManager
      │                                           │
      │  2. ServerMessage::Registered { ... }     │
      │ ◄─────────────────────────────────────────
      │                                           │
      │  3. ServerMessage::WorldState (初始状态)   │
      │ ◄─────────────────────────────────────────
      │                                           │
      │  [心跳任务启动: 每30秒 Ping]                │
      │                                           │
      │  4. ClientMessage::Intent                 │
      │  ─────────────────────────────────────────►
      │                                           │
      │                                           ├──► handle_intent()
      │                                           │         │
      │                                           │         ├──► rate_limit 检查
      │                                           │         ├──► is_alive 检查
      │                                           │         ├──► tick_id 校验
      │                                           │         │
      │                                           │         ├──► 即时动作判断
      │                                           │         │    (speak/whisper/emote)
      │                                           │         │
      │                                           │         ├──► speak → 立即广播
      │                                           │         │    broadcast_speak_to_location()
      │                                           │         │
      │                                           │         ├──► whisper → 立即创建 Session
      │                                           │         │    dialogue_manager.create_session()
      │                                           │         │
      │                                           │         └──► 保存到 IntentManager
      │                                           │
      │  5. ServerMessage::ImmediateEvent (speak) │
      │ ◄─────────────────────────────────────────
      │  (同场景所有在线 Agent 立即收到)           │
      │                                           │
      │  6. ClientMessage::Intent (whisper)       │
      │  ─────────────────────────────────────────►
      │                                           │
      │  7. DialogueMessage::Accept { session }   │
      │ ◄─────────────────────────────────────────
      │
      │  [心跳响应: Ping/Pong]                     │
      │                                           │
      │  8. 连接断开                               │
      │ ─────────────────────────────────────────►
      │                                           │
      │                                           ├──► ConnectionManager.remove()
      │                                           └──► agent_to_device_map.remove()
```

### 4.3 即时事件系统 (Immediate Event System)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         即时事件系统 (Speak 广播)                            │
└─────────────────────────────────────────────────────────────────────────────┘

    Agent A                                     Server                          Agent B (同场景)
      │                                           │                                │
      │  speak intent (ClientMessage)              │                                │
      │  action_type: "speak"                      │                                │
      │  action_data: { content: "你好" }          │                                │
      │ ─────────────────────────────────────────►                                │
      │                                           │                                │
      │                                           ├──► handle_intent()             │
      │                                           │         │                      │
      │                                           │         ├──► 检测到即时动作     │
      │                                           │         │                      │
      │                                           │         └──► tokio::spawn()     │
      │                                           │              (独立任务)        │
      │                                           │                   │             │
      │                                           │                   ▼             │
      │                                           │    broadcast_speak_to_location()
      │                                           │              │                 │
      │                                           │              ▼                 │
      │  ServerMessage::ImmediateEvent {         │    ┌─────────────────┐        │
      │    event: { speak, content: "你好" }     │    │ 同一 Location    │        │
      │  }                                        │    │ 的在线 Agent     │        │
      │ ◄─────────────────────────────────────────    └────────┬────────┘        │
      │                                                       │                  │
      │                                                       ▼                  │
      │                                                       Agent B 收到       │
      │                                                                       │
      │  [speak intent 标记为 already_broadcast=true]                         │
      │  [在 tick 结算时不再重复处理]                                          │
```

### 4.4 死亡通知系统 (Death Notification)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         死亡通知系统                                         │
└─────────────────────────────────────────────────────────────────────────────┘

    Server Tick Engine                              Agent
      │                                               │
      │  decay::apply_decay_and_env_damage()          │
      │         │                                      │
      │         ├──► HP <= 0 检测                      │
      │         │                                      │
      │         ▼                                      │
      │  ┌─────────────────────────────────────────┐  │
      │  │         死亡处理流程                     │  │
      │  │                                        │  │
      │  │  1. 构建 DeathNotification             │  │
      │  │     ├── cause: "hunger" | "thirst"    │  │
      │  │     ├── description: 叙事化文本         │  │
      │  │     ├── location: node_id             │  │
      │  │     ├── died_at: Unix ms              │  │
      │  │     └── rebirth_delay_ticks          │  │
      │  │                                        │  │
      │  │  2. send_agent_died_notification()    │  │
      │  │     └── WebSocket 推送                 │  │
      │  │                                        │  │
      │  │  3. 断开 WebSocket 连接                │  │
      │  │     └── ConnectionManager.remove()     │  │
      │  │                                        │  │
      │  │  4. 清空背包 + 死亡掉落                │  │
      │  │     └── InventoryManager.clear()      │  │
      │  │         + add_ground_item()           │  │
      │  │                                        │  │
      │  │  5. UPDATE agents.status = 'dead'    │  │
      │  │                                        │  │
      │  └─────────────────────────────────────────┘  │
      │         │                                      │
      │         ▼                                      │
      │  ServerMessage::AgentDied { ... }             │
      │ ◄──────────────────────────────────────────────
      │                                               │
      │  [Agent 透传给 OpenClaw，触发重生流程]         │
```

### 4.5 对话 (Dialogue) 流程

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           对话流程                                            │
└─────────────────────────────────────────────────────────────────────────────┘

    Agent A                                     Server                          Agent B
      │                                           │                                │
      │  whisper intent                           │                                │
      │  action_data: { target_agent_id, content }│                                │
      │  ─────────────────────────────────────────►                                │
      │                                           │                                │
      │                                           ├──► dialogue_manager.create_session()
      │                                           │         │                      │
      │                                           │         ├──► 验证双方状态      │
      │                                           │         └──► 创建 Session       │
      │                                           │              │                 │
      │                                           │              ▼                 │
      │                                           │    ┌─────────────────┐        │
      │                                           │    │ DialogueSession  │        │
      │                                           │    │ state: Active    │        │
      │                                           │    └────────┬────────┘        │
      │                                           │              │                 │
      │                                           │              ▼                 │
      │  DialogueMessage::Accept { session_id }   │    forward_dialogue_message() │
      │ ◄─────────────────────────────────────────    │    (WebSocket send)          │
      │                                           │    └────────┬────────┘        │
      │                                           │              │                 │
      │  DialogueMessage::Content { content }     │              ▼                 │
      │  ─────────────────────────────────────────►    Agent B 收到消息           │
      │                                           │                                │
      │                                           ├──► dialogue_manager.handle_message()
      │                                           │                                │
      │  DialogueMessage::Content { reply }       │                                │
      │ ◄─────────────────────────────────────────                                │
      │                                           │                                │
      │                                           ├──► close_all_sessions()       │
      │                                           │    (Tick 结算时)              │
      │                                           │                                │
      │  ServerMessage::PrivateDialogueRecord     │                                │
      │ ◄─────────────────────────────────────────                                │
```

---

## 五、孤立节点分析

### 5.1 孤立模块 (未接入主流程)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              孤立节点                                        │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────┐     ┌─────────────────────┐     ┌─────────────────────┐
│    game_data/      │     │    items/          │     │    agent/           │
│    formula_engine/  │     │    system.rs       │     │    component/       │
│                     │     │                     │     │    social/          │
│  🟡 FormulaEngine   │     │  🔴 apply_item_    │     │                     │
│  DerivedAttribute   │     │  effect()          │     │  🟡 Relationship    │
│  Component 使用     │     │  #[allow(dead_     │     │  Store              │
│  但 combat/decay    │     │  code)] 死代码      │     │  API 层查询关系     │
│  热路径绕过它       │     │  物品效果在         │     │  但 binary 从未     │
│  直接用 evalexpr    │     │  StateChange 中     │     │  初始化 (None)      │
│                     │     │  内联处理           │     │                     │
└─────────────────────┘     └─────────────────────┘     └─────────────────────┘

┌─────────────────────┐     ┌─────────────────────┐
│    agent/           │     │    protocol/        │
│    core/tools.rs    │     │    sqlx_types.rs    │
│                     │     │                     │
│  🟡 #[allow(dead_   │     │  🔴 未被 Server    │
│  code)] 未被调用    │     │  端使用            │
│  Cognitive 用自带的 │     │                     │
│  ActorToolExecutor  │     │  可选 sqlx 支持    │
│  Claw 也不用        │     │  用于类型推断       │
└─────────────────────┘     └─────────────────────┘
```

### 5.2 潜在孤立管道

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            孤立管道                                          │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ 1. 物品耐久度衰减管道 (TODO)                                                 │
│                                                                              │
│   decay.rs 中注释:                                                           │
│   ```                                                                       │
│   // TODO: 实现物品自然损坏逻辑 (Phase 2)                                     │
│   // 1. 获取所有 Agent 的背包                                                │
│   // 2. 对每个物品，如果有 decay_rate > 0，则减少 durability                   │
│   // 3. 如果 durability <= 0，则移除物品                                     │
│   ```                                                                       │
│                                                                              │
│   状态: 🔴 未实现 (isolated)                                                 │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ 2. Status Effects 管道                                                      │
│                                                                              │
│   WorldState.self_state.status_effects                                       │
│                                                                              │
│   broadcaster.rs 中注释:                                                     │
│   ```                                                                       │
│   // 注意：status_effects 字段暂未实现，始终为空数组                          │
│   // Agent 的实际状态效果通过 attribute_descriptions 描述                     │
│   ```                                                                       │
│                                                                              │
│   状态: 🟡 部分实现 (仅描述文本)                                              │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ 3. Nearby Items (场景物品) 管道                                              │
│                                                                              │
│   完整管道已接通:                                                             │
│   - drop/death → add_ground_item → ground_items 表                          │
│   - pick_up/gather → remove_ground_item → InventoryManager                  │
│   - broadcast: get_ground_items_by_nodes (batch) → SceneItem → WorldState   │
│                                                                              │
│   注意: build_initial_world_state() 仍返回空 nearby_items，                  │
│   Agent 连接后需等下一个 Tick 广播才能看到地面物品                            │
│                                                                              │
│   状态: ✅ 已集成                                                            │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ 4. Agent LLM Client 热重载管道                                               │
│                                                                              │
│   agent/src/runtime/claw/bridge.rs                                          │
│                                                                              │
│   LlmClientContainer (Arc<RwLock<Arc<dyn LlmClient>>>)                      │
│   ActorSoul 和 ReflectorSoul 共享同一容器                                    │
│   config_reload_rx → lifecycle.rs select! → 读新配置 → swap 容器             │
│   Builder 正确传递容器: with_llm_container → with_llm_client 复用            │
│                                                                              │
│   状态: ✅ 已集成                                                            │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ 5. 验证错误通知管道 (v0.3 新增)                                               │
│                                                                              │
│   StateProcessor 返回 validation_errors → Scheduler 发送 ServerMessage::Error│
│                                                                              │
│   流程:                                                                      │
│   - process_intents() 返回 validation_errors                                 │
│   - scheduler 遍历 validation_errors                                         │
│   - broadcaster::send_to_agent() → ServerMessage::Error                     │
│                                                                              │
│   状态: ✅ 已集成                                                            │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 六、数据存储结构

### 6.1 数据库表关系

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              PostgreSQL Schema                               │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────┐         ┌─────────────────┐         ┌─────────────────┐
│     devices     │         │     agents     │         │  agent_states  │
├─────────────────┤         ├─────────────────┤         ├─────────────────┤
│ id (PK)         │         │ agent_id (PK)   │◄────────│ agent_id (FK)   │
│ device_id (UK)  │◄────────│ device_id (FK)  │         │ id (PK)         │
│ auth_token      │         │ name            │         │ tick_id         │
│ created_at      │         │ system_prompt   │         │ attributes (JSONB)
│ last_seen_at    │         │ status          │         │ node_id         │
└────────┬────────┘         │ created_at      │         │ is_alive        │
         │                  │ retired_at      │         │ created_at      │
         │                  └────────┬────────┘         └────────┬────────┘
         │                           │                           │
         │                           │                           │
         │                  ┌────────┴────────┐                  │
         │                  │ agent_inventory │                  │
         │                  ├─────────────────┤                  │
         └─────────────────►│ id (PK)         │                  │
                            │ agent_id (FK)   │◄─────────────────┘
                            │ item_id (FK)   │
                            │ quantity        │
                            │ is_equipped     │
                            └─────────────────┘

┌─────────────────┐         ┌─────────────────┐
│   tick_logs    │         │  agent_actions  │
├─────────────────┤         ├─────────────────┤
│ id (PK)         │         │ id (PK)         │
│ tick_id (UK)   │         │ tick_id (FK)   │
│ status          │         │ agent_id (FK)   │
│ started_at      │         │ action_type     │
│ completed_at    │         │ action_data     │
│ agents_processed│         │ result          │
│ actions_executed│         │ thought_log     │
└─────────────────┘         └─────────────────┘

┌─────────────────┐
│   ground_items  │
├─────────────────┤
│ id (PK)         │
│ node_id         │
│ item_id (FK)   │
│ quantity        │
│ dropped_by (FK) │
│ created_at      │
└─────────────────┘
```

### 6.2 内存数据结构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              内存数据结构                                     │
└─────────────────────────────────────────────────────────────────────────────┘

AppState (Arc)
├── db_pool: PgPool
├── connection_manager: ConnectionManager
│   └── HashMap<device_id, Connection>
│       └── Connection { agent_id, device_id, sender, is_dead }
├── agent_to_device_map: AgentToDeviceMap
│   └── HashMap<agent_id, device_id>
├── intent_manager: IntentManager
│   └── HashMap<agent_id, Intent>
├── rate_limiter: RateLimiter
│   └── HashMap<agent_id, Instant>
├── game_data_cache: Arc<GameDataCache>
│   ├── game_rules: GameRulesConfig
│   ├── actions: ActionsConfig
│   ├── attributes: UnifiedAttributesConfig
│   ├── items: ItemsConfig
│   ├── locations: LocationsConfig
│   ├── recipes: RecipesConfig
│   ├── display_messages: DisplayMessagesConfig
│   ├── narrative: NarrativeConfig
│   └── location_registry: LocationRegistry
│       └── HashMap<node_id, LocationNode>
├── dialogue_manager: Arc<DialogueManager>
│   └── sessions: RwLock<SessionRegistry>
│       └── HashMap<session_id, DialogueSession>
├── current_accepting_tick_id: AtomicI64
├── admin_read_token: String
├── admin_write_token: String
└── start_time: DateTime<Utc>

Agent (crate/agent)
├── config: Config
├── client: AgentClient (WebSocket)
├── memory_manager: Option<MemoryManager>
│   ├── working_backend: WorkingMemoryBackend
│   ├── episodic_backend: EpisodicMemoryBackend
│   ├── semantic_backend: SemanticMemoryBackend
│   └── forgetting_scheduler: ForgettingScheduler
├── validator: Option<Arc<dyn Validator>>
├── lifespan_calculator: Option<LifespanCalculator>
├── relationship_store: Option<RelationshipStore>
└── actor_llm_container: Option<LlmClientContainer>
```

---

## 七、配置系统

### 7.1 配置加载流程

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          配置加载流程                                         │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                              Server 启动                                     │
└─────────────────────────────────────────────────────────────────────────────┘
         │
         ├──► paths::get_config_dir()  [获取配置目录]
         │         │
         │         ├──► 开发环境: `crates/server/config/`
         │         └──► 生产环境: `config/` (Docker)
         │
         ├──► game_data::load_game_data()  [加载游戏数据]
         │         │
         │         ├──► loaders::load_game_rules()
         │         ├──► loaders::load_actions()
         │         ├──► loaders::load_attributes()
         │         ├──► loaders::load_items()
         │         ├──► loaders::load_locations()
         │         ├──► loaders::load_recipes()
         │         ├──► loaders::load_display_messages()
         │         ├──► loaders::load_narrative()
         │         └──► loaders::load_time()
         │
         ├──► GameDataCache::new(game_data)  [创建缓存]
         │
         ├──► init_registry(cache)  [初始化全局注册表]
         │         │
         │         ├──► ActionRegistry::init()
         │         ├──► ItemRegistry::init()
         │         ├──► StateRegistry::init()
         │         ├──► TimeRegistry::init()
         │         └──► InitialInventoryRegistry::init()
         │
         └──► items::init_item_cache_from_config()  [初始化物品缓存]
```

### 7.2 配置热重载

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          配置热重载                                          │
└─────────────────────────────────────────────────────────────────────────────┘

scheduler.run()
     │
     ├──► check_and_reload_actions()  [每 Tick 检查]
     │         │
     │         ├──► fs::metadata(actions.yaml)
     │         │         │
     │         │         ├──► modified > last_mtime? → 重新加载
     │         │         │
     │         │         └──► load_actions() → new_actions
     │         │
     │         ├──► game_data_cache.update_actions(new_actions)
     │         │
     │         ├──► init_registry(cache)  [重新初始化注册表]
     │         │
     │         └──► broadcast_action_update()  [广播给所有 Agent]
     │                   │
     │                   └──► ServerMessage::ActionUpdate
     │
     └──► /api/admin/reload-config  [手动触发]
               │
               └──► handlers::config_reload::reload_config_handler()
                        │
                        └──► 同上流程
```

---

## 八、错误处理流程

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            错误处理流程                                      │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                           Server 端错误                                      │
└─────────────────────────────────────────────────────────────────────────────┘

WebSocket Handler
     │
     ├──► 消息解析错误
     │         │
     │         └──► ServerMessage::Error { code: "invalid_message", ... }
     │
     ├──► Intent 处理错误
     │         │
     │         ├──► verify_device_token() 失败 → "Unauthorized"
     │         ├──► rate_limit 触发 → "rate_limited"
     │         ├──► Agent 已死亡 → "agent_dead"
     │         ├──► tick_id 不匹配 → "tick_mismatch"
     │         ├──► 纵深防御: agents.status != 'active' → "agent_dead"
     │         └──► 执行失败 → "action_failed"
     │
     └──► 对话处理错误
               │
               └──► ServerMessage::Error { code: "dialogue_failed", ... }

┌─────────────────────────────────────────────────────────────────────────────┐
│                           Tick 错误                                          │
└─────────────────────────────────────────────────────────────────────────────┘

scheduler.run()
     │
     ├──► broadcast_new_tick() 失败 → 记录日志，继续下一 Tick
     │
     ├──► execute_tick_settlement() 失败
     │         │
     │         ├──► tick_log.fail()
     │         ├──► update_tick_log() 失败 → 记录日志
     │         └──► save_tick_log() 失败 → 记录日志
     │
     └──► 单 Tick 耗时 > 10s → warn 日志

┌─────────────────────────────────────────────────────────────────────────────┐
│                        验证错误通知 (v0.3 新增)                               │
└─────────────────────────────────────────────────────────────────────────────┘

StateProcessor.process_intents()
     │
     ├──► IntentResolver::validate() 失败
     │         │
     │         └──► validation_errors.push((agent_id, reason))
     │
     └──► 返回 validation_errors

scheduler.execute_tick_inner()
     │
     └──► 遍历 validation_errors
               │
               └──► broadcaster::send_to_agent()
                        │
                        └──► ServerMessage::Error

┌─────────────────────────────────────────────────────────────────────────────┐
│                           Agent 端错误                                      │
└─────────────────────────────────────────────────────────────────────────────┘

AgentClient
     │
     ├──► WebSocket 连接断开
     │         │
     │         ├──► 重连逻辑 (指数退避)
     │         └──► 达到最大重试 → 退出
     │
     ├──► 收到 ServerMessage::Error
     │         │
     │         ├──► "agent_dead" → 记录死亡状态
     │         ├──► "tick_mismatch" → 等待下一 Tick
     │         └──► 其他错误 → 记录日志
     │
     └──► 意图审查驳回
               │
               └──► last_rejection_reason → 传递给下一轮决策
```

---

## 九、关键数据结构转换

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          数据结构转换                                        │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ 1. WorldState 构建流程                                                      │
│                                                                              │
│  DB Row (agent_states)                                                      │
│       │                                                                      │
│       ▼                                                                      │
│  AgentState (models/agent.rs)                                                │
│       │                                                                      │
│       ├──► primary_attributes: AttributeComponent                            │
│       │         └──► AttributeCollection → JSONB attributes                   │
│       │                                                                      │
│       ├──► status: StatusComponent                                          │
│       │         └──► AttributeCollection → JSONB attributes                   │
│       │                                                                      │
│       ▼                                                                      │
│  WorldState (protocol/types/world.rs)                                        │
│       │                                                                      │
│       ├──► self_state: AgentSelfState                                       │
│       │         ├──► attributes: HashMap<String, i32>                       │
│       │         ├──► derived_attributes: HashMap<String, f32>               │
│       │         └──► attribute_descriptions: HashMap<String, String>         │
│       │                     (from NarrativeConfig)                            │
│       │                                                                      │
│       ├──► entities: Vec<Entity>                                             │
│       │         (同节点在线 Agent)                                            │
│       │                                                                      │
│       ├──► nearby_items: Vec<SceneItem>                                      │
│       │         (地面物品)                                                   │
│       │                                                                      │
│       └──► events_log: Vec<WorldEvent>                                      │
│                 (本 Tick 事件)                                                │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ 2. Intent 提交流程                                                          │
│                                                                              │
│  Agent 决策                                                                  │
│       │                                                                      │
│       ▼                                                                      │
│  ActorSoul Intent (crate/agent)                                              │
│       │                                                                      │
│       ├──► action_type: String                                              │
│       ├──► action_data: Option<serde_json::Value>                            │
│       ├──► thought_log: Option<String>                                      │
│       └──► intent_id: Option<Uuid>                                          │
│                                                                              │
│       ▼                                                                      │
│  ClientMessage::Intent (protocol/messages.rs)                                │
│       │                                                                      │
│       ├──► intent_id: Option<Uuid>                                          │
│       ├──► tick_id: i64                                                     │
│       ├──► agent_id: Option<Uuid>                                           │
│       ├──► thought_log: Option<String>                                      │
│       ├──► action_type: String                                              │
│       ├──► action_data: Option<serde_json::Value>                           │
│       └──► priority: i32                                                     │
│                                                                              │
│       ▼                                                                      │
│  Intent (server/models/actions.rs)                                           │
│       │                                                                      │
│       ├──► IntentManager (HashMap)                                          │
│       │         └──► 用于 tick 结算                                          │
│       │                                                                      │
│       └──► ActionExecutor::execute()                                       │
│                 │                                                            │
│                 └──► ActionExecutionResult                                  │
│                           │                                                 │
│                           ├──► state_changes: Vec<StateChange>              │
│                           └──► success: bool                                 │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ 3. Tick ID 计算 (v0.3 改进)                                                  │
│                                                                              │
│  tick_id = current_unix_timestamp - game_epoch                              │
│                                                                              │
│  game_epoch 解析:                                                            │
│  - 从 game_rules.yaml 读取 start_date 和 timezone_offset                     │
│  - 例如: start_date: "2026-03-03", timezone_offset: 8 (UTC+8)                 │
│  - 结果: 2026-03-03 00:00:00 UTC+8 = 2026-03-02 16:00:00 UTC                 │
│         = Unix timestamp 1772467200                                         │
│                                                                              │
│  时钟回拨防御:                                                               │
│  - tick_id 只增不减 (使用 max() 比较)                                        │
│  - 数据库最大 tick_id 与时间计算 tick_id 取较大值                            │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 十、附录：模块清单

### 10.1 Server Crate 模块

| 模块 | 文件数 | 描述 | 状态 |
|------|--------|------|------|
| tick | 12 | Tick 引擎核心 | ✅ 完整 |
| tick/processor | 6 | 状态处理子模块 | ✅ 完整 |
| websocket | 5 | WebSocket 通信 | ✅ 完整 |
| handlers | 9 | HTTP API 处理器 | ✅ 完整 |
| game_data | 25+ | 数据驱动配置 | ✅ 完整 |
| models | 11 | 数据模型 | ✅ 完整 |
| actions | 5 | 动作系统 | ✅ 完整 |
| db | 5 | 数据库操作 | ✅ 完整 |
| dialogue | 4 | 对话系统 | ✅ 完整 |
| inventory | 4 | 背包管理 | ✅ 完整 |
| items | 5 | 物品系统 | 🟡 部分 (system.rs 死代码) |
| config | 1 | 配置加载 | ✅ 完整 |
| state | 1 | 应用状态 | ✅ 完整 |
| paths | 1 | 路径工具 | ✅ 完整 |

### 10.2 Agent Crate 模块

| 模块 | 文件数 | 描述 | 状态 |
|------|--------|------|------|
| soul/actor | 5 | 认知引擎 | ✅ 完整 |
| soul/reflector | 7 | 审查引擎 | ✅ 完整 |
| component/memory | 15 | 三级记忆 | ✅ 完整 |
| component/persona | 7 | 人设系统 | ✅ 完整 |
| component/social | 7 | 社交系统 | 🟡 部分 (RelationshipStore 未初始化) |
| component/llm | 3 | LLM 抽象 | ✅ 完整 |
| infra/api | 9 | HTTP API | ✅ 完整 |
| infra/transport | 2 | WebSocket | ✅ 完整 |
| runtime/cognitive | 1 | 认知模式 | ✅ 完整 |
| runtime/claw | 8 | Claw 模式 | ✅ 完整 |
| core | 6 | Agent 核心 | ✅ 完整 |

### 10.3 Protocol Crate 模块

| 模块 | 文件数 | 描述 | 状态 |
|------|--------|------|------|
| types/* | 10 | 数据类型 | ✅ 完整 |
| messages | 1 | 消息定义 | ✅ 完整 |
| error | 1 | 错误类型 | ✅ 完整 |

---

*文档生成时间: 2026-04-09*
